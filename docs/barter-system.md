# Barter System

Barter rules live in `barter_rules`.

Each rule may define:

- NPC or faction owner
- offered payload
- requested payload
- condition payload
- success modifier payload

Phase J exposes barter simulation only. Real inventory mutation belongs to a later save/inventory runtime.
